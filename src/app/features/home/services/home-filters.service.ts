import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class HomeFiltersService {
  /**
   * Placeholder for future filter configuration logic.
   */
  getDefaultFilters(): Record<string, never> {
    return {};
  }
}
